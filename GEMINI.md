# Database Scripts Rule

Whenever you create or modify any SQL database queries, migrations, or schema changes, you MUST add them as a new `.sql` script file inside the `database_script/` directory.

DO NOT create loose `.sql` files in the root directory.
